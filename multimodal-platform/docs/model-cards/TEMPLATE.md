# Model card: <adapter name>

- Base model / adapter type / rank:
- Training data (dataset, subset size, split, manifest hash):
- Config (link to configs/*.yaml + W&B run):
- Metrics (frozen eval slice + full benchmark):
- Intended use / limitations / known failure modes:
- License:
