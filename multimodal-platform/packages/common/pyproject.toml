[project]
name = "mmp-common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7", "structlog>=24.1", "pillow>=10.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mmp_common"]
