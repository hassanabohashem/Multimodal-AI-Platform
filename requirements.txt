# Multimodal AI Platform — consolidated requirements (PyTorch side)
# Covers: gateway + embeddings service + training + shared common + dev tools.
# NOTE: the OCR service is NOT here on purpose — PaddlePaddle must not share
# an environment with PyTorch. Use requirements-ocr.txt in its own venv/container.
#
# Install:  pip install -r requirements.txt
# (Preferred workflow is still `uv sync --all-packages`, which uses the
#  per-package pyproject.toml files and a lockfile.)

# --- API / gateway ---
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
slowapi>=0.1.9
prometheus-client>=0.20
qdrant-client>=1.14
structlog>=24.1
pillow>=10.3

# --- ML / embeddings / training ---
# GPU wheels: install torch first per https://pytorch.org for your CUDA version, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.7
transformers>=4.52
trl>=0.18
peft>=0.15
bitsandbytes>=0.45        # Linux/CUDA; on Windows laptops without GPU you can skip it
accelerate>=1.6
datasets>=3.0
timm>=1.0
tokenizers>=0.20
numpy>=1.26

# --- experiment tracking / eval ---
wandb>=0.17
pycocoevalcap
pyyaml>=6.0

# --- dev tools ---
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
pre-commit>=3.7
